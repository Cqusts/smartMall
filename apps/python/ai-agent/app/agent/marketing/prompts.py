"""运营文案提示词。

两条贯穿全篇的约束，和另外几个 Agent 是同一条线：

* **只能用给定的属性和需求信号**，不许编成分、编数值
* **不许用极限词**——这一条写了模型也会犯，所以后面有规则复查
  （``compliance.py``）。提示词是请求，不是约束
"""

from __future__ import annotations

POINTS_SYSTEM = """你是电商运营，负责提炼商品卖点。只输出 json，不要解释。"""

POINTS_USER = """给这件商品挑出最该讲的卖点。

商品属性（**只能用这里面的事实**）：
{attrs}

规格与价格：
{skus}

用户在客服那边**实际问得最多**的问题：
{demand}

挑 3 到 5 条卖点，每条 15 字以内。要求：

- **用户反复问的，优先做成卖点。** 有人一直问"会不会起球"，
  那"抗起球"就该排在前面——那是他真正犹豫的地方。
  运营想当然的"高级感"排后面
- 每条要能对应到上面某个属性或某个提问，**对应不上的不要写**
- 说人话，不要"匠心工艺""臻选品质"这种空话

每条标注来源：``attr`` 来自属性表，``demand`` 来自用户提问。

输出 json：
{{"points": [{{"text": "抗起球，洗过不炸毛", "source": "demand"}},
             {{"text": "100%羊毛，320g 克重", "source": "attr"}}]}}"""


WRITE_SYSTEM = """你是电商文案。只输出 json，不要解释。"""

WRITE_USER = """给这件商品写一整套文案。

商品：{name}（{category}）
属性（**只能用这里面的事实**）：
{attrs}
规格与价格：
{skus}

已经定好的卖点，围绕它们写：
{points}
{audience_hint}{style_hint}
一次输出这几种形态：

- ``title``：商品标题，30 字以内，含核心关键词
- ``main_images``：主图角标文案 3 条，每条 12 字以内
- ``points``：详情页卖点短句 5 条，每条 15 字以内
- ``detail``：详情长文案，200 到 400 字
- ``script``：30 秒短视频口播脚本

**几条硬规矩：**

1. **不许编成分。** 属性表里写的是什么就是什么。写"羊绒"而属性是
   "羊毛"，那是虚假宣传，不是修辞
2. **不许编数字。** 克重、含量、尺寸、折扣，属性表里没有的一个都不许出现
3. **不许用极限词。** 最好、最佳、第一、顶级、国家级、唯一、百分百……
   《广告法》第九条明令禁止，会被处罚
4. **不许说功效。** 治疗、消炎、排毒这类，服装类目说了就是违规
5. 各种形态之间**说法要一致**：标题说羊毛，详情就不能说混纺

输出 json，字段名照上面写。"""


AUDIENCE_HINT = "\n目标人群：{audience}（按他们在意的点组织语言）\n"
STYLE_HINT = "\n风格：{style}\n"
