# evals —— 评测集与评测脚本

13 个评测集，与代码同仓版本管理。详见 [docs/12-eval-observability.md](../docs/12-eval-observability.md)。

## 硬性约束

**评测集样本绝不能出现在训练集中。**
`dataset_version.filter_sql` 必须包含
`session_id NOT IN (SELECT session_id FROM eval_sample)`，
否则会出现"在训练集上评测"的经典错误——指标虚高但线上无效。

## 评测集清单

| 评测集 | 规模 | 检验 | 里程碑 |
|---|---|---|---|
| `eval-retrieval` | 300 | Recall@5 ≥ 0.85 | M2 |
| `eval-answer` | 200 | 忠实度 ≥ 0.90 | M2 |
| `eval-negative` | 100 | 拒答准确率 ≥ 0.90 | M2 |
| `eval-multimodal` | 100 | 多模态链路 | M4 |
| `eval-intent` | 300 | 意图分类 ≥ 0.85 | M2 |
| `eval-style` | 200 | 盲评「像真人」≥ 40% | M7 |
| `eval-rag-faithfulness` | 200 | 🔴 防退化红线 | M7 |
| `eval-instruction` | 100 | 防退化 | M7 |
| `eval-safety` | 100 | 违禁词漏出 = 0 | M2 |
| `eval-hallucination` | 100 | 幻觉检测 | M2 |
| `eval-asr` | 50 | 商品名识别 ≥ 0.9 | M5 |
| `eval-segment` | 30 | 分段准确率 ≥ 0.8 | M5 |
| `kpi-golden-set` | 300–500 | Judge 校准 ρ ≥ 0.7 | M6 |

`baseline.json` 存放各指标的当前基线，CI 门禁以此比对（低于基线 3% 则失败）。

---

## 检索召回：单路 vs 混合（已跑出数）

```bash
cd apps/python/ai-rag
EMBEDDING_ONNX_DIR=<模型目录> python -m app.eval_cli --sweep 0.05,0.1,0.2,0.5,1.0 --holdout
```

评测集 `retrieval.jsonl`（115 条查询）+ `retrieval-corpus.jsonl`（100 条语料）。
查询手写，**刻意与语料用词不同**——照抄库里的措辞的话 BM25 必赢，三档对比就没有
意义。按难度分三类：`plain` 56 条、`lexical` 42 条（同义不同词，卡 BM25）、
`exact` 17 条（型号/数字/专名，卡向量）。

### 结果（bge-small-zh-v1.5 + Milvus Lite，2026-08-29）

| 检索方式 | R@1 | R@3 | R@5 | R@10 | MRR@10 |
|---|---|---|---|---|---|
| 纯向量 | 0.852 | 0.948 | **0.974** | 0.991 | **0.903** |
| 纯 BM25 | 0.583 | 0.748 | 0.826 | 0.896 | 0.679 |
| RRF 等权融合 | 0.730 | 0.904 | 0.957 | 0.974 | 0.824 |
| RRF 加权（稀疏 0.10） | 0.843 | 0.965 | 0.991 | 1.000 | 0.903 |

**结论：这个评测集上，混合检索没有可主张的收益。**

- **等权 RRF 比纯向量差**（R@5 −0.017，MRR −0.079）。机理：一条只被向量召回、
  排第 3 的文档得分 `1/(60+3)=0.0159`，而两路都平庸（第 20 与第 15）的文档得
  `1/80+1/75=0.0258` —— **"两边都还行"压过了"一边非常对"**。实测样例
  「晚上十一点还有人吗」向量排第 3、BM25 完全召不回（库里写的是 9:00-22:00），
  等权融合把它压到第 13。
- **给稀疏路降权到 0.10 能修好这个压制**，但留出验证（在 57 条上选权重、报另外
  58 条）显示加权融合 R@5 1.000 vs 纯向量 0.983 —— **差 1 条查询，不显著**。

### 这份数字的边界，别越过它说话

1. **评测集偏向向量**：`lexical` 42 条 vs `exact` 17 条是手写时的失衡，
   而 `lexical` 正是 BM25 的死穴。
2. **语料只有 100 条且每条只讲一件事**。BM25 靠稀有词的 IDF 吃饭，
   小语料 + 短文档给不出这个信号；真实知识库里的长文档对它有利得多。
3. **天花板太低**：纯向量 R@5 已经 0.974，只剩 3 条可改进，
   任何方法的提升上限就是 2.6 个百分点。**要让这个对比有信息量，
   得先把评测集做难**（更大语料、更多易混条目）。
4. 换向量化后端数字就会变。这里用的是 91MB 的 bge-small-zh；
   线上默认是 `text-embedding-v4`，跑 `--embedding dashscope` 才是生产数。

---

## 兜底闸门：转人工率 / 漏放率 / 误转率

```bash
cd apps/python/ai-rag
EMBEDDING_ONNX_DIR=<模型目录> python -m app.eval_cli --gate
```

正样本 = `retrieval.jsonl` 的 115 条（语料答得了），
负样本 = `retrieval-negative.jsonl` 的 40 条（语料**答不了**，按同一份语料手写）。

> ⚠️ 不能用 `negative.jsonl` 顶替。那份是照项目自己的知识库写的，
> 对这份 100 条评测语料来说，其中「花呗分期」「增值税专票」「会员等级」
> 「寄香港」「企业采购」都是**答得了**的——拿它量会把正确回答记成漏放。

判定复刻 `graph.route_after_retrieve` 的三个出口，**不调 LLM**：
闸门本身是纯阈值逻辑，为了量它去付一次模型调用，只会把「闸门判错」
和「模型答错」混进同一个数。

### 结果（bge-small-zh-v1.5 + Milvus Lite，2026-08-29）

| | 直答 | 澄清 | 转人工 | 共 |
|---|---|---|---|---|
| 答得了 | 102（0.887） | 1 | 12（0.104） | 115 |
| 答不了 | 4（0.100） | 5 | 31（0.775） | 40 |

- **转人工率 0.277**（155 条里 43 条交给人；澄清不计——它没占用人力）
- **漏放率 0.100**（答不了的 40 条里直答了 4 条）　← 危险的那一端
- **误转率 0.104**（答得了的 115 条里白弹给人工 12 条）　← 纯损失的那一端

**两端必须一起报。** 全部转人工能把漏放率刷成 0，全部直答能把误转率刷成 0，
任何一端单独拿出来都可以靠调死阈值刷满分。

4 条漏放全部落在 0.55–0.63 这个窄带里（「明天会降价吗」0.602、
「运费险怎么买」0.569、「能开个人抬头的专票吗」0.557、「支持先用后付吗」0.560），
共同形态是**问到了一个已有条目的邻近细节**——语料讲运费、用户问运费险。
这类靠调高 `clarify_below` 挡不住（会把误转率一起推上去），
要靠补知识或加一层拒答判断。

### 阈值副本会漂，靠测试盯着

评测里的三个阈值是 ai-agent `AgentConfig` 的副本。第一版写成"try 从
`app.agent.nodes` 导入，导不到退回硬编码"，看着像从源头取值——
**实际永远走不到 try 分支**：两个服务的顶层包都叫 `app`，
`app.agent` 在 ai-rag 进程里解析的是 ai-rag 自己的包。那段导入是死代码。
现在明写成常量，另配一条按路径读 `nodes.py` 的漂移测试
（`TestGateThresholds::test_gate_thresholds_match_the_agent`），那边改了这边就挂。
